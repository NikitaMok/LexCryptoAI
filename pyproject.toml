[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
markers = [
    "no_pipeline_stub: не подменять внешние вызовы в app.pipeline",
]

[tool.ruff]
line-length = 100
target-version = "py311"
